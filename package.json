{
  "name": "pointless-lang.github.io",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node --watch-path=. build-scripts/build.js",
    "fmt": "deno fmt",
    "lint": "deno lint; stylelint static/css/*",
    "serve": "serve --config site/serve.json"
  },
  "devDependencies": {
    "@prettier/sync": "^0.6.1",
    "command-line-args": "^6.0.1",
    "csv-parse": "^5.6.0",
    "gray-matter": "^4.0.3",
    "marked": ">=15",
    "marked-highlight": "^2.2.1",
    "serve": "^14.2.4",
    "stylelint": "^16.23.0",
    "stylelint-config-standard": "^39.0.0"
  },
  "stylelint": {
    "extends": "stylelint-config-standard",
    "rules": {
      "font-family-name-quotes": null,
      "no-descending-specificity": null
    }
  }
}
